#include <stdio.h>

/**
 * main - function prints single digits number in base 10, starting from 0
 *
 * Return: 0 when success
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
