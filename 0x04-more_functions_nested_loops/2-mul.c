#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first parameter to be considered
 * @b: second parameter to be considered
 * Return: a * b
 */

int mul(int a, int b)
{
	return (a * b);
}
